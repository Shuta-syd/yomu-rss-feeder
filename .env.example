# 必須: DB内のAPIキー等を暗号化するキー。openssl rand -hex 32 で生成
ENCRYPTION_KEY=

# 任意: SQLite DB ファイルパス (デフォルト: data/yomu.db)
# DATABASE_PATH=data/yomu.db

# 任意: 本番モードでのみ secure cookie を有効化する用
# NODE_ENV=development
